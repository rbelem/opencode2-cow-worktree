# e2e parallel-agent swarm run — 2026-09-12

Produced by four parallel `fast-generic` agents, each driving its own real
`opencode2 serve` instance against the repo checkout at `dcc8ce1`. Every value
below is observed from live runs, not hand-written. Worker evidence archives:
`/tmp/opencode/swarm-cow/w{1..4}/REPORT.md` (volatile scratch; key values are
recorded here).

## Environment

- server binary: `opencode2 v0.0.0-next-20260912.3` (devbox; the `.2` binary of
  `run-2026-09-12.md` was the last version previously exercised)
- server runtime (observed): bun single-file build
- worker ports: 46181–46184 (one server per worker, isolated `OPENCODE_DB`,
  throwaway projects under `/tmp/opencode/swarm-cow/`)
- filesystems: source and worktree targets on btrfs; `/dev/shm` used as the
  cross-device / non-CoW negative case
- repo state: `dcc8ce1` ("fix: refuse to delete a dirty cow worktree without
  force"), clean tree; unit suite re-run inside the swarm: 179 pass / 0 fail,
  390 expect() calls, 17 files; `tsc --noEmit` clean

## Slices and results

31 scenarios, 31 PASS, 0 issues.

### W1 — Deep-clone create path, isolation, extent sharing (6/6 PASS)

- `POST /api/worktree` with no `strategy` field, three in parallel: 200 in
  25–27 ms each; 320 ms wall-clock for all three.
- Clone integrity (each of w1/w2/w3): ignored `node_modules/dep/index.js`
  present, ignored `debug.log` present, `link-to-tracked` is a symlink,
  `.git` is a real directory with no `objects/info/alternates`, `git status`
  matches source, `git log` functional.
- Isolation: each worktree sees only its own marker and log line; edits in
  siblings and in the source invisible everywhere else.
- `GET /api/worktree` lists all three as `strategy: "cow"`.
- Extent sharing (`sync` + `btrfs filesystem du -s --raw`): every clone
  17,256,448 bytes **shared**, 0 exclusive; the `cp -a --reflink=never`
  control shows 0 shared, 17,256,448 exclusive. Real reflink, not a byte copy.
- No deviations from the `run-2026-09-12.md` (`.2`) baseline.

### W2 — Remove path and the dirty-worktree guard (#13) (8/8 PASS)

- Clean remove at `force: false`: 204, directory deleted.
- Dirty tracked edit, `force: false`: 400, directory **and** edit intact;
  refusal body names the changed paths.
- Same dirty worktree at `force: true`: 204, deleted.
- Untracked-file-only, `force: false`: 400, directory and new file intact.
- Rename-only dirty (porcelain `R` line), `force: false`: 400 — renames count
  as dirty.
- Unknowable state (`.git` removed from the worktree), `force: false`: 400,
  fail-closed, exact `mayRemove` message for `undefined` uncommitted state.
- Refusal precedes deletion: readdir before/after a refusal is identical; no
  partial deletion.
- No contract deviations from `src/strategy.ts` / `src/dirty.ts`.

Notes: the probe's `git status --porcelain` is spawned via `execFile` inside
the plugin and does not appear in server logs; probe execution is inferred
from the refusal body. The structured error's `forceRequired: null` remains
the known upstream wrapper artifact (see `docs/e2e/findings.md` #14), not a
plugin deviation.

### W3 — `spawn_workspace` tool and options surface (9/9 PASS)

Driven through a real server session (`OPENCODE_SIMULATE=1` +
`OPENCODE_DRIVE=1`), one throwaway config per scenario.

- CoW success: `Created cow worktree at … (session ses_…)`, session exists
  server-side, `.git` is a directory (Deep clone).
- `fallback: "none"` + tmpfs source: fails loud — *"copy-on-write is not
  supported for … and no fallback is enabled"* — identical wording to the
  `.2` baseline; no worktree directory left behind.
- `fallback: "git"` + tmpfs source: `Created git worktree at …`, `.git` is a
  file (Shallow), session exists, removed cleanly.
- `options.targetRoot` on a different device: error names the filesystem
  mismatch and the reflink device rule — not a bare `EXDEV`; nothing left on
  the target device.
- `options.targetRoot` valid (same device): worktree lands under the
  configured root.
- Invalid options throw on invocation: `invalid plugin option "fallback":
  expected one of none, git, got "yolo"`, `invalid plugin option
  "targetRoot": expected a non-empty path string, got 123`.
- Result text carries `mechanism`, `directory`, `sessionID`; input schema
  matches `src/plugin.ts`.
- No deviations from the `.2` baseline formats.

### W4 — Fresh-binary install, defaults, packaging (8/8 PASS)

- Isolated directory-plugin install: plugin `active` via `GET /api/plugin`
  (entry `id: "opencode2-cow-worktree"`, `state.status: "active"`); unit
  sweep at HEAD green (see Environment).
- No-`strategy` create produces a Deep clone: `cow` is the Location default
  on `.3`.
- `worktree.directory: ".opencode/worktrees"` resolves against the project;
  nested target created; no target-into-self recursion.
- Explicit `strategy: "git"` still selects built-in git (`.git` is a file);
  inventory reports `git` and `cow` correctly per entry.
- `DELETE` (with force) removes all three; inventory empties to the source.
- `bun pm pack --dry-run`: `tui.tsx` (1.88 KB) and `strategy-badge.ts`
  (0.85 KB) included; `private: true`.
- Server log scan vs `.2` run: zero new plugin-load warnings/errors.

## Observations (non-defects)

1. `.3` logs the plugin load as `msg="loading plugin" id=<directory path>`
   — the plugin directory path, not the `opencode2-cow-worktree` id. Binary-
   side log-format change; activation state via `GET /api/plugin` is
   unaffected.
2. W2's harness overrode `OPENCODE_CONFIG_DIR` but not `HOME`, so its server
   resolved the global config from the live user seam
   (`~/.config/opencode/plugins/opencode2-cow-worktree`) instead of an
   isolated copy. Same plugin source (symlink to this repo at `dcc8ce1`), so
   the W2 verdicts stand; W4 independently proved the isolated-load path on
   `.3`.
3. A pre-existing, unrelated `opencode2 serve --service` process (started
   before the swarm, port 49374) was left untouched.

## What this run did not prove

Same limits as `run-2026-09-12.md`: no real multi-agent LLM fan-out (no
provider credentials; filesystem and tool behavior only), the interactive TUI
surface (badge rendering) was not exercised headless, and the v2 transcript
exposes tool state as text, so the raw structured `output` object was
verified via the declared schema plus the runtime text.
