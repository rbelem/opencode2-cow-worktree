# AGENTS.md

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `rbelem/opencode2-cow-worktree`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, unchanged label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo: `CONTEXT.md` at the root plus ADRs in `docs/adr/`. See `docs/agents/domain.md`.
